using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using UnityEngine;
using UnityEngine.UI;

public class DebugCanvas : MonoBehaviour
{
    //protected override bool IsPersistant
    //{
    //    get { return true; }
    //}
    //public Text DebugText;
    //StringBuilder sb = new StringBuilder();
    //private void Update()
    //{
    //    if (LaserSharpGameloader.MainGameConfig.ShowDebugLog)
    //    {
    //        sb.Clear();
    //        sb.AppendLine("Last InterstitialTime : " + LaserSharpAdsManager.LastInterstitialAdTime);
    //        sb.AppendLine("Time Since LastInterstitial Ad : " + DateTime.Now.Subtract(LaserSharpAdsManager.LastInterstitialAdTime).TotalSeconds);
    //        sb.AppendLine("Last AdTime : " + LaserSharpAdsManager.LastAdTime);
    //        sb.AppendLine("NumberOfGamesPlayedSinceLastInterstitialAd : " + LaserSharpAdsManager.NumberOfGamesPlayedSinceLastAd);
    //        sb.AppendLine("Time BetweenAds : " + LaserSharpAdsManager.TimeBetweenAds);
    //        if (DebugText.text != sb.ToString())
    //            DebugText.SetText(sb.ToString());
    //    }
    //}
}
